os: linux
dist: xenial

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y libdbus-glib-1-dev

language: python
python: "3.5"

branches:
    only: master

matrix:
    include:
        - env: TASK=lint
        - env: TASK=fmt-travis

script: make -f Makefile $TASK
